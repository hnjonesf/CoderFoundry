@model SquadClock.Models.Manager

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Manager</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Company.ApplicationUserId)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Company.ApplicationUserId)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.ManagerTitle)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.ManagerTitle)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
